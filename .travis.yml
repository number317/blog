language: go

os:
  - linux

matrix:
  allow_failures:
    - go: tip
  fast_finish: true

script:
  - ./hugo

# Deploy to GitHub pages
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: public
  github_token: $GH_TOKEN
  on:
    branch: master
